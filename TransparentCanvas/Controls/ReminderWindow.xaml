<Window x:Class="TransparentCanvas.Controls.ReminderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="⏰ 日程提醒" 
        Height="280" Width="420"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="NoResize">
    
    <Border CornerRadius="12" 
            Background="#F0252526" 
            BorderBrush="#FF0078D4" 
            BorderThickness="2">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="4" Opacity="0.6" BlurRadius="16" Color="#000000"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 顶部图标和标题 -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="⏰" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="日程提醒" 
                               Foreground="#FF0078D4" 
                               FontSize="18" 
                               FontWeight="Bold"/>
                    <TextBlock x:Name="TimeText" 
                               Foreground="#AAAAAA" 
                               FontSize="11"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
            
            <!-- 分隔线 -->
            <Border Grid.Row="1" Height="1" Background="#444" Margin="0,0,0,12"/>
            
            <!-- 笔记标题 -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock x:Name="NoteTitleText" 
                           Foreground="White" 
                           FontSize="20" 
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="时间到了！" 
                           Foreground="#FFD700" 
                           FontSize="14" 
                           TextAlignment="Center"
                           Margin="0,8,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
            
            <!-- 确认按钮 -->
            <Button Grid.Row="3" 
                    x:Name="DismissButton"
                    Content="我知道了" 
                    Width="120" Height="36"
                    HorizontalAlignment="Center"
                    Margin="0,12,0,0"
                    Background="#0078D4"
                    Foreground="White"
                    FontSize="14"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="DismissButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="16,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#1A8AD4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>